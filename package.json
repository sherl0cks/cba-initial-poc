{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "lerna:bootstrap": "lerna bootstrap --since origin/master",
    "lerna:test": "lerna run test --since origin/master",
    "lerna:build": "lerna run build --since origin/master",
    "lerna:publish": "lerna publish --yes"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
